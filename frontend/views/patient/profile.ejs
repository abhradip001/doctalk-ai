<%- include('../shared/header') %>

<main class="max-w-5xl mx-auto mt-10 bg-white shadow rounded-lg p-6">
  <% const isEdit = (typeof mode !== 'undefined' && mode === 'edit'); %>

  <% if (saved) { %>
    <div class="mb-4 rounded border border-green-200 bg-green-50 text-green-800 px-4 py-3">
      ✓ Profile saved successfully.
    </div>
  <% } %>

  <% if (!isEdit) { %>
    <!-- READ MODE -->
    <div class="flex items-center justify-between mb-6">
      <h1 class="text-2xl font-bold text-blue-600">My Profile</h1>
      <a href="/patient/profile?edit=1"
         class="bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700">Edit</a>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-x-10 gap-y-4">
      <div>
        <div class="text-gray-500 text-sm">Name</div>
        <div class="font-medium"><%= patient.name || '-' %></div>
      </div>
      <div>
        <div class="text-gray-500 text-sm">Email</div>
        <div class="font-medium"><%= patient.email || '-' %></div>
      </div>
      <div>
        <div class="text-gray-500 text-sm">Age</div>
        <div class="font-medium"><%= (patient.age ?? '') !== '' ? patient.age : '-' %></div>
      </div>
      <div>
        <div class="text-gray-500 text-sm">Sex</div>
        <div class="font-medium"><%= patient.sex || 'N/A' %></div>
      </div>
      <div>
        <div class="text-gray-500 text-sm">Blood Group</div>
        <div class="font-medium"><%= patient.bloodGroup || 'N/A' %></div>
      </div>
      <div>
        <div class="text-gray-500 text-sm">Phone</div>
        <div class="font-medium"><%= patient.phone || '-' %></div>
      </div>
      <div class="md:col-span-2">
        <div class="text-gray-500 text-sm">Address</div>
        <div class="font-medium whitespace-pre-line"><%= patient.address || 'N/A' %></div>
      </div>
    </div>
  <% } else { %>
    <!-- EDIT MODE -->
    <div class="flex items-center justify-between mb-6">
      <h1 class="text-2xl font-bold text-blue-600">Edit Profile</h1>
      <a href="/patient/profile" class="text-blue-600 hover:underline">Cancel</a>
    </div>

    <form action="/patient/profile" method="POST" class="space-y-5">
      <div>
        <label class="block font-semibold mb-1">Name</label>
        <input type="text" name="name" value="<%= patient.name || '' %>"
               class="border p-2 w-full rounded focus:ring-2 focus:ring-blue-500 focus:outline-none" />
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        <div>
          <label class="block font-semibold mb-1">Age</label>
          <input type="number" name="age" min="0" value="<%= patient.age || '' %>"
                 class="border p-2 w-full rounded focus:ring-2 focus:ring-blue-500 focus:outline-none" />
        </div>
        <div>
          <label class="block font-semibold mb-1">Sex</label>
          <select name="sex" class="border p-2 w-full rounded focus:ring-2 focus:ring-blue-500 focus:outline-none">
            <option value="">-- Select --</option>
            <option value="Male"   <%= patient.sex === 'Male' ? 'selected' : '' %>>Male</option>
            <option value="Female" <%= patient.sex === 'Female' ? 'selected' : '' %>>Female</option>
            <option value="Other"  <%= patient.sex === 'Other' ? 'selected' : '' %>>Other</option>
          </select>
        </div>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        <div>
          <label class="block font-semibold mb-1">Blood Group</label>
          <select name="bloodGroup" class="border p-2 w-full rounded focus:ring-2 focus:ring-blue-500 focus:outline-none">
            <option value="">-- Select --</option>
            <% ['A+','A-','B+','B-','AB+','AB-','O+','O-'].forEach(bg => { %>
              <option value="<%= bg %>" <%= patient.bloodGroup === bg ? 'selected' : '' %>><%= bg %></option>
            <% }) %>
          </select>
        </div>
        <div>
          <label class="block font-semibold mb-1">Phone</label>
          <input type="text" name="phone" value="<%= patient.phone || '' %>"
                 class="border p-2 w-full rounded focus:ring-2 focus:ring-blue-500 focus:outline-none" />
        </div>
      </div>

      <div>
        <label class="block font-semibold mb-1">Address</label>
        <textarea name="address" rows="3"
                  class="border p-2 w-full rounded focus:ring-2 focus:ring-blue-500 focus:outline-none"><%= patient.address || '' %></textarea>
      </div>

      <div class="pt-2">
        <button type="submit"
                class="bg-blue-600 text-white px-6 py-2 rounded-lg font-semibold hover:bg-blue-700 transition">
          Save Changes
        </button>
      </div>
    </form>
  <% } %>
</main>

<%- include('../shared/footer') %>
