<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title><%= typeof title !== 'undefined' ? title : 'Not Found' %></title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 min-h-screen flex items-center justify-center">
  <div class="text-center">
    <h1 class="text-3xl font-bold text-red-600 mb-2">404 — Page not found</h1>
    <p class="text-gray-600 mb-6">
      We couldn’t find:
      <span class="font-mono"><%= typeof url !== 'undefined' ? url : '' %></span>
    </p>
    <div class="flex gap-3 justify-center">
      <a href="/" class="bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700">Go Home</a>
    </div>
  </div>
</body>
</html>
