<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login to Clinic-AI</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-r from-blue-100 via-purple-100 to-pink-100 flex items-center justify-center h-screen">
    <div class="bg-white shadow-2xl rounded-2xl w-full max-w-md p-8 transition-transform transform hover:scale-105">
        <h2 class="text-3xl font-extrabold text-center mb-6 text-gray-800">Login to Clinic-AI</h2>

        <% if (error) { %>
        <div class="bg-red-100 text-red-700 p-3 rounded mb-4 text-sm font-medium shadow">
            <%= error %>
        </div>
        <% } %>

        <form action="/auth/login" method="POST" class="space-y-5">
            <div>
                <label for="email" class="block text-gray-700 font-semibold mb-1">Email</label>
                <input type="email" id="email" name="email" 
                       class="w-full border border-gray-300 rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-blue-400 shadow-sm"
                       placeholder="Enter your email" required>
            </div>

            <div>
                <label for="password" class="block text-gray-700 font-semibold mb-1">Password</label>
                <input type="password" id="password" name="password" 
                       class="w-full border border-gray-300 rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-blue-400 shadow-sm"
                       placeholder="Enter your password" required>
            </div>

            <div>
                <label for="role" class="block text-gray-700 font-semibold mb-1">Login As</label>
                <select id="role" name="role" 
                        class="w-full border border-gray-300 rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-blue-400 shadow-sm" required>
                    <option value="patient">Patient</option>
                    <option value="doctor">Doctor</option>
                    <option value="admin">Admin</option>
                </select>
            </div>

            <button type="submit"
                    class="w-full bg-gradient-to-r from-blue-500 to-purple-500 text-white py-2 rounded-lg font-semibold hover:from-blue-600 hover:to-purple-600 transition-all shadow-md">
                Login
            </button>
        </form>

        <p class="mt-6 text-center text-gray-600 text-sm">
            Don’t have an account? <br>
            <a href="/patients/register" class="text-green-500 hover:underline font-semibold">Patient</a> | 
            <a href="/doctors/register" class="text-purple-500 hover:underline font-semibold">Doctor</a> | 
            <a href="/admins/register" class="text-red-500 hover:underline font-semibold">Admin</a>
        </p>
    </div>
</body>
</html>
